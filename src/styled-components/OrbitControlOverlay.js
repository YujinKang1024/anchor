import styled from 'styled-components';

const OrbitControlOverlay = styled.div`
  position: absolute;
  top: 41vh;
  left: 43.2vw;
  width: 14vw;
  height: 19vh;
  pointer-events: auto;
  background-color: rgba(1, 1, 1, 0.5);
  z-index: 20;
`;

export default OrbitControlOverlay;
