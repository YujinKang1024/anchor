import { atom } from 'jotai';

export const isScrollingAtom = atom(false);
export const isReturningAtom = atom(false);
export const targetRotationAtom = atom(0);
export const scrollPositionAtom = atom(0);
