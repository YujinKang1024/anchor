import { Html } from '@react-three/drei';
import OrbitControlOverlay from '../../styled-components/OrbitControlOverlay';

export default function OrbitControlArea() {
  return (
    <Html center>
      <OrbitControlOverlay />
    </Html>
  );
}
